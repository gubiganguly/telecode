# === Auth ===
TELECODE_AUTH_PASSWORD=change-me-to-a-strong-password
TELECODE_AUTH_SECRET=change-me-to-a-random-64-char-hex-string

# === URLs (replace yourdomain.com with your actual domain) ===
TELECODE_CORS_ORIGINS=https://yourdomain.com
TELECODE_FRONTEND_URL=https://yourdomain.com
TELECODE_GITHUB_CALLBACK_URL=https://yourdomain.com/api/github/auth/callback

# Frontend (build-time)
NEXT_PUBLIC_WS_URL=wss://yourdomain.com/ws/chat

# === GitHub OAuth (optional — only if you use the GitHub integration) ===
TELECODE_GITHUB_CLIENT_ID=
TELECODE_GITHUB_CLIENT_SECRET=

# === Cloudflare Tunnel ===
TUNNEL_TOKEN=your-tunnel-token-from-cloudflare-dashboard
